{
  "name": "course-management-backend",
  "version": "1.0.0",
  "description": "Online Course Management System - Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:setup": "psql -f schema.sql && psql -f seed.sql",
    "db:schema": "psql -f schema.sql",
    "db:seed": "psql -f seed.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}